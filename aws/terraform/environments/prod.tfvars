# Production Environment Configuration

# DNS is managed in GoDaddy, not Route 53
use_route53 = false

# Landing page subdomains not needed (users use custom domains instead)
enable_landing_page_subdomains = false
