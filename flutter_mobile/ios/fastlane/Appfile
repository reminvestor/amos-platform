# App Store Connect App Identifier
app_identifier(ENV["APP_IDENTIFIER"] || "com.amoslabs.mobile")

# Apple ID for App Store Connect
apple_id(ENV["APPLE_ID"])

# Team ID for your Apple Developer account
itc_team_id(ENV["ITC_TEAM_ID"])
team_id(ENV["TEAM_ID"])
