<!DOCTYPE html>
<html>
  <head>
    <title>Admin Portal - AMOS Labs</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link rel="stylesheet" href="/assets/admin_dark.css" data-turbo-track="reload">
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>

    <!-- Lucide Icons -->
    <script src="https://unpkg.com/lucide@latest"></script>
  </head>

  <body class="admin-dark">
    <% if @current_admin %>
      <%# Left Sidebar Navigation %>
      <aside class="admin-sidebar" id="adminSidebar">
        <%# Logo with Hamburger %>
        <div class="sidebar-logo">
          <div class="logo-icon">
            <i data-lucide="box" class="icon-xl"></i>
          </div>
          <span class="logo-text">AMOS Admin</span>
          <button class="hamburger-toggle" id="sidebarToggle" aria-label="Toggle sidebar">
            <i data-lucide="menu"></i>
          </button>
        </div>

        <%# Main Navigation %>
        <nav>
          <%= link_to admin_dashboard_path, class: "nav-item #{'active' if current_page?(admin_dashboard_path)}" do %>
            <i data-lucide="layout-dashboard"></i>
            <span>Dashboard</span>
          <% end %>

          <%= link_to admin_services_path, class: "nav-item #{'active' if current_page?(admin_services_path)}" do %>
            <i data-lucide="activity"></i>
            <span>Services Health</span>
          <% end %>

          <%= link_to admin_users_path, class: "nav-item #{'active' if controller_name == 'users'}" do %>
            <i data-lucide="users"></i>
            <span>Users</span>
          <% end %>

          <%= link_to admin_integrations_path, class: "nav-item #{'active' if controller_name == 'integrations'}" do %>
            <i data-lucide="plug"></i>
            <span>Integrations</span>
          <% end %>

          <%= link_to admin_connections_path, class: "nav-item #{'active' if controller_name == 'connections'}" do %>
            <i data-lucide="link"></i>
            <span>Connections</span>
          <% end %>

          <%= link_to admin_pipeline_connections_path, class: "nav-item #{'active' if request.path.start_with?('/admin/pipeline')}" do %>
            <i data-lucide="git-branch"></i>
            <span>AI Pipeline</span>
          <% end %>

          <%= link_to admin_parallel_tasks_path, class: "nav-item #{'active' if controller_name == 'parallel_tasks'}" do %>
            <i data-lucide="layers"></i>
            <span>Parallel Tasks</span>
          <% end %>

          <div class="nav-group #{'active' if request.path.start_with?('/admin/observability')}">
            <div class="nav-item nav-group-header">
              <i data-lucide="bar-chart-3"></i>
              <span>Observability</span>
              <i data-lucide="chevron-down" class="chevron-icon"></i>
            </div>
            <div class="nav-submenu">
              <%= link_to admin_observability_ai_usage_path, class: "nav-subitem #{'active' if current_page?(admin_observability_ai_usage_path)}" do %>
                <span>AI Usage</span>
              <% end %>
              <%= link_to admin_observability_workflows_path, class: "nav-subitem #{'active' if current_page?(admin_observability_workflows_path)}" do %>
                <span>Workflows</span>
              <% end %>
              <%= link_to admin_observability_performance_path, class: "nav-subitem #{'active' if current_page?(admin_observability_performance_path)}" do %>
                <span>Performance</span>
              <% end %>
              <%= link_to admin_observability_errors_path, class: "nav-subitem #{'active' if current_page?(admin_observability_errors_path)}" do %>
                <span>Errors</span>
              <% end %>
            </div>
          </div>

          <%= link_to dashboard_admin_agent_lightning_index_path, class: "nav-item #{'active' if request.path.start_with?('/admin/agent_lightning')}" do %>
            <i data-lucide="zap"></i>
            <span>Agent Lightning</span>
          <% end %>
        </nav>

        <%# Bottom Actions %>
        <div class="sidebar-bottom">
          <button data-theme-toggle class="nav-item w-100 text-start border-0 bg-transparent" aria-label="Toggle light/dark theme">
            <i data-theme-icon data-lucide="moon" aria-hidden="true"></i>
            <span>Theme</span>
          </button>

          <%= link_to chat_mode_path, class: "nav-item" do %>
            <i data-lucide="home"></i>
            <span>Return to App</span>
          <% end %>

          <%= button_to admin_destroy_session_path, method: :delete, class: "nav-item", form: { data: { turbo: false } } do %>
            <i data-lucide="log-out"></i>
            <span>Sign Out</span>
          <% end %>
        </div>
      </aside>

      <%# Flash Messages %>
      <% if notice.present? || alert.present? %>
        <div style="position: fixed; top: 5rem; right: 1rem; z-index: 9999; max-width: 500px;">
          <% if notice %>
            <div class="admin-alert admin-alert-success" style="margin-bottom: 1rem; box-shadow: 0 10px 25px rgba(0, 0, 0, 0.3);">
              <div class="alert-content">
                <i data-lucide="check-circle"></i>
                <span><%= notice %></span>
              </div>
              <button class="alert-close" onclick="this.parentElement.remove(); lucide.createIcons();">×</button>
            </div>
          <% end %>
          <% if alert %>
            <div class="admin-alert admin-alert-error" style="margin-bottom: 1rem; box-shadow: 0 10px 25px rgba(0, 0, 0, 0.3);">
              <div class="alert-content">
                <i data-lucide="alert-circle"></i>
                <span><%= alert %></span>
              </div>
              <button class="alert-close" onclick="this.parentElement.remove(); lucide.createIcons();">×</button>
            </div>
          <% end %>
        </div>
      <% end %>

      <%# Main Content Area %>
      <%= yield %>

    <% else %>
      <!-- Login page content -->
      <%= yield %>
    <% end %>

    <!-- Initialize Lucide Icons -->
    <script>
      document.addEventListener('DOMContentLoaded', () => {
        lucide.createIcons();
        initSidebarToggle();
        initNavGroups();
      });

      document.addEventListener('turbo:load', () => {
        lucide.createIcons();
        initSidebarToggle();
        initNavGroups();
      });

      function initSidebarToggle() {
        const sidebar = document.getElementById('adminSidebar');
        const toggle = document.getElementById('sidebarToggle');

        if (!sidebar || !toggle) return;

        // Check saved state
        const isCollapsed = localStorage.getItem('adminSidebarCollapsed') === 'true';
        if (isCollapsed) {
          sidebar.classList.add('collapsed');
        }

        // Toggle on click
        toggle.addEventListener('click', () => {
          sidebar.classList.toggle('collapsed');
          localStorage.setItem('adminSidebarCollapsed', sidebar.classList.contains('collapsed'));

          // Re-initialize icons after DOM changes
          setTimeout(() => lucide.createIcons(), 100);
        });
      }

      function initNavGroups() {
        // Handle nav group dropdowns
        document.querySelectorAll('.nav-group-header').forEach(header => {
          header.addEventListener('click', (e) => {
            const navGroup = header.parentElement;
            navGroup.classList.toggle('expanded');

            // Re-initialize icons after DOM changes
            setTimeout(() => lucide.createIcons(), 100);
          });
        });

        // Auto-expand active nav groups
        document.querySelectorAll('.nav-group.active').forEach(group => {
          group.classList.add('expanded');
        });
      }
    </script>
  </body>
</html>
