<!-- Chart.js (lazy-loaded local version, ~68KB gzipped) -->
<!-- Replaced CDN with optimized local bundle for better performance and offline support -->
<%= javascript_include_tag "chart_loader", type: "module", defer: true %>
