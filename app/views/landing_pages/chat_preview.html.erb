<%# This is an empty template file that renders inside landing_page_chat layout %>
<%# The layout already contains all necessary content %>

<%# The rollback option is already included in the layout, so we don't need to add it here %> 