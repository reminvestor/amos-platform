<div class="email-template-editor" data-template-id="<%= email_template.id %>" style="background-color: #0A0E1A; color: #FFFFFF; min-height: 100vh; padding: 2rem;">
  <!-- Page Header -->
  <div style="display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: 2rem; flex-wrap: wrap; gap: 1rem;">
    <div>
      <h1 style="color: #FFFFFF; font-size: 2rem; font-weight: 700; margin-bottom: 0.5rem; display: flex; align-items: center; gap: 1rem;">
        <i data-lucide="mail" style="width: 2rem; height: 2rem;"></i>
        <%= email_template.name %>
      </h1>
      <p style="color: #94A3B8; margin-bottom: 0;">Last updated <%= time_ago_in_words(email_template.updated_at) %> ago</p>
    </div>
    <div style="display: flex; gap: 0.5rem; flex-wrap: wrap;">
      <button id="saveTemplateChanges" onclick="saveTemplateChanges()" aria-label="Save template changes" style="background: rgb(74, 222, 128); color: white; border: none; padding: 0.5rem 1rem; border-radius: 0.375rem; cursor: pointer; display: inline-flex; align-items: center; gap: 0.5rem; font-weight: 500;">
        <i data-lucide="save" style="width: 1rem; height: 1rem;"></i>
        Save Changes
      </button>
      <button onclick="sendTestEmail()" aria-label="Send test email" style="background: linear-gradient(135deg, #7C3AED 0%, #A78BFA 100%); color: white; border: none; padding: 0.5rem 1rem; border-radius: 0.375rem; cursor: pointer; display: inline-flex; align-items: center; gap: 0.5rem; font-weight: 500;">
        <i data-lucide="send" style="width: 1rem; height: 1rem;"></i>
        Send Test
      </button>
      <button onclick="window.scoutSendMessage?.('Improve this email template to be more engaging and professional')" aria-label="Improve template with AI" style="background: rgb(250, 204, 21); color: #000000; border: none; padding: 0.5rem 1rem; border-radius: 0.375rem; cursor: pointer; display: inline-flex; align-items: center; gap: 0.5rem; font-weight: 500;">
        <i data-lucide="wand-2" style="width: 1rem; height: 1rem;"></i>
        Improve with AI
      </button>
      <button onclick="window.scoutLoadCanvas?.('email_template_viewer', {})" aria-label="Back to template list" style="background: transparent; color: #22D3EE; border: 1px solid #22D3EE; padding: 0.5rem 1rem; border-radius: 0.375rem; cursor: pointer; display: inline-flex; align-items: center; gap: 0.5rem; font-weight: 500;">
        <i data-lucide="arrow-left" style="width: 1rem; height: 1rem;"></i>
        Back to List
      </button>
    </div>
  </div>

  <!-- Instructions Alert -->
  <div style="background: linear-gradient(135deg, #7C3AED 0.5%, #1A1F35 100%); border: 1px solid #1E293B; border-radius: 0.75rem; padding: 1.5rem; margin-bottom: 2rem;">
    <div style="display: flex; gap: 1rem;">
      <div style="width: 3rem; height: 3rem; background: rgba(124, 58, 237, 0.2); border-radius: 0.75rem; display: flex; align-items: center; justify-content: center; flex-shrink: 0;">
        <i data-lucide="info" style="width: 1.5rem; height: 1.5rem; color: #A78BFA;"></i>
      </div>
      <div>
        <h5 style="color: #FFFFFF; font-weight: 600; margin-bottom: 0.5rem;">Email Template Editor</h5>
        <ul style="color: #94A3B8; margin: 0; padding-left: 1.5rem; font-size: 0.875rem;">
          <li>Edit the subject line and email content below</li>
          <li>Use <strong>{{first_name}}</strong>, <strong>{{last_name}}</strong>, <strong>{{email}}</strong> for personalization</li>
          <li>Click "Improve with AI" to enhance your content</li>
          <li>Send a test email to preview before using in campaigns</li>
        </ul>
      </div>
    </div>
  </div>

  <!-- Email Template Form -->
  <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 2rem;">
    <!-- Left Column: Editor -->
    <div>
      <div style="background: #1A1F35; border: 1px solid #1E293B; border-radius: 0.75rem; overflow: hidden;">
        <div style="padding: 1.5rem;">
          <!-- Subject Line -->
          <div style="margin-bottom: 1.5rem;">
            <label for="templateSubject" style="color: #FFFFFF; font-weight: 600; display: flex; align-items: center; gap: 0.5rem; margin-bottom: 0.5rem;">
              <i data-lucide="heading" style="width: 1rem; height: 1rem;"></i>
              Subject Line
            </label>
            <input type="text"
                   id="templateSubject"
                   value="<%= email_template.subject %>"
                   placeholder="Enter email subject..."
                   aria-label="Email subject line"
                   style="width: 100%; background: #0F172A; border: 1px solid #1E293B; border-radius: 0.375rem; padding: 0.75rem; color: #FFFFFF; font-size: 0.875rem;">
          </div>

          <!-- Email Body -->
          <div style="margin-bottom: 1.5rem;">
            <label style="color: #FFFFFF; font-weight: 600; display: flex; align-items: center; justify-content: space-between; gap: 1rem; margin-bottom: 0.75rem;">
              <span style="display: flex; align-items: center; gap: 0.5rem;">
                <i data-lucide="file-text" style="width: 1rem; height: 1rem;"></i>
                Email Content
              </span>
              <div style="display: flex; gap: 0.25rem;">
                <button type="button" id="wysiwygMode" onclick="switchToWysiwyg()" aria-label="Visual editor mode" style="background: linear-gradient(135deg, #7C3AED 0%, #A78BFA 100%); color: white; border: none; padding: 0.375rem 0.75rem; border-radius: 0.375rem; cursor: pointer; display: inline-flex; align-items: center; gap: 0.25rem; font-size: 0.875rem; font-weight: 500;">
                  <i data-lucide="eye" style="width: 0.875rem; height: 0.875rem;"></i>
                  Visual
                </button>
                <button type="button" id="htmlMode" onclick="switchToHtml()" aria-label="HTML editor mode" style="background: transparent; color: #22D3EE; border: 1px solid #22D3EE; padding: 0.375rem 0.75rem; border-radius: 0.375rem; cursor: pointer; display: inline-flex; align-items: center; gap: 0.25rem; font-size: 0.875rem; font-weight: 500;">
                  <i data-lucide="code" style="width: 0.875rem; height: 0.875rem;"></i>
                  HTML
                </button>
              </div>
            </label>

            <!-- WYSIWYG Editor -->
            <div id="wysiwygEditor" style="display: block;">
              <!-- Formatting Toolbar -->
              <div style="background: #0F172A; border: 1px solid #1E293B; border-radius: 0.375rem; padding: 0.75rem; margin-bottom: 0.5rem; display: flex; gap: 0.5rem; flex-wrap: wrap;">
                <div style="display: flex; gap: 0.25rem; border-right: 1px solid #1E293B; padding-right: 0.5rem;">
                  <button type="button" onclick="formatText('bold')" aria-label="Bold" style="background: transparent; color: #22D3EE; border: 1px solid #22D3EE; padding: 0.375rem 0.5rem; border-radius: 0.25rem; cursor: pointer; display: inline-flex; align-items: center; gap: 0.25rem;">
                    <i data-lucide="bold" style="width: 0.875rem; height: 0.875rem;"></i>
                  </button>
                  <button type="button" onclick="formatText('italic')" aria-label="Italic" style="background: transparent; color: #22D3EE; border: 1px solid #22D3EE; padding: 0.375rem 0.5rem; border-radius: 0.25rem; cursor: pointer; display: inline-flex; align-items: center; gap: 0.25rem;">
                    <i data-lucide="italic" style="width: 0.875rem; height: 0.875rem;"></i>
                  </button>
                  <button type="button" onclick="formatText('underline')" aria-label="Underline" style="background: transparent; color: #22D3EE; border: 1px solid #22D3EE; padding: 0.375rem 0.5rem; border-radius: 0.25rem; cursor: pointer; display: inline-flex; align-items: center; gap: 0.25rem;">
                    <i data-lucide="underline" style="width: 0.875rem; height: 0.875rem;"></i>
                  </button>
                </div>
                <div style="display: flex; gap: 0.25rem; border-right: 1px solid #1E293B; padding-right: 0.5rem;">
                  <button type="button" onclick="formatText('justifyLeft')" aria-label="Align left" style="background: transparent; color: #22D3EE; border: 1px solid #22D3EE; padding: 0.375rem 0.5rem; border-radius: 0.25rem; cursor: pointer; display: inline-flex; align-items: center; gap: 0.25rem;">
                    <i data-lucide="align-left" style="width: 0.875rem; height: 0.875rem;"></i>
                  </button>
                  <button type="button" onclick="formatText('justifyCenter')" aria-label="Align center" style="background: transparent; color: #22D3EE; border: 1px solid #22D3EE; padding: 0.375rem 0.5rem; border-radius: 0.25rem; cursor: pointer; display: inline-flex; align-items: center; gap: 0.25rem;">
                    <i data-lucide="align-center" style="width: 0.875rem; height: 0.875rem;"></i>
                  </button>
                  <button type="button" onclick="formatText('justifyRight')" aria-label="Align right" style="background: transparent; color: #22D3EE; border: 1px solid #22D3EE; padding: 0.375rem 0.5rem; border-radius: 0.25rem; cursor: pointer; display: inline-flex; align-items: center; gap: 0.25rem;">
                    <i data-lucide="align-right" style="width: 0.875rem; height: 0.875rem;"></i>
                  </button>
                </div>
                <div style="display: flex; gap: 0.25rem; border-right: 1px solid #1E293B; padding-right: 0.5rem;">
                  <button type="button" onclick="formatText('insertUnorderedList')" aria-label="Bullet list" style="background: transparent; color: #22D3EE; border: 1px solid #22D3EE; padding: 0.375rem 0.5rem; border-radius: 0.25rem; cursor: pointer; display: inline-flex; align-items: center; gap: 0.25rem;">
                    <i data-lucide="list" style="width: 0.875rem; height: 0.875rem;"></i>
                  </button>
                  <button type="button" onclick="formatText('insertOrderedList')" aria-label="Numbered list" style="background: transparent; color: #22D3EE; border: 1px solid #22D3EE; padding: 0.375rem 0.5rem; border-radius: 0.25rem; cursor: pointer; display: inline-flex; align-items: center; gap: 0.25rem;">
                    <i data-lucide="list-ordered" style="width: 0.875rem; height: 0.875rem;"></i>
                  </button>
                </div>
                <div style="display: flex; gap: 0.25rem; border-right: 1px solid #1E293B; padding-right: 0.5rem;">
                  <select onchange="formatText('formatBlock', this.value); this.value='';" aria-label="Select heading level" style="background: #0F172A; color: #94A3B8; border: 1px solid #1E293B; padding: 0.375rem; border-radius: 0.25rem; font-size: 0.875rem;">
                    <option value="">Heading...</option>
                    <option value="h1">Heading 1</option>
                    <option value="h2">Heading 2</option>
                    <option value="h3">Heading 3</option>
                    <option value="p">Normal</option>
                  </select>
                </div>
                <div style="display: flex; gap: 0.25rem;">
                  <button type="button" onclick="insertLink()" aria-label="Insert link" style="background: transparent; color: #22D3EE; border: 1px solid #22D3EE; padding: 0.375rem 0.5rem; border-radius: 0.25rem; cursor: pointer; display: inline-flex; align-items: center; gap: 0.25rem;">
                    <i data-lucide="link" style="width: 0.875rem; height: 0.875rem;"></i>
                  </button>
                </div>
              </div>

              <!-- Content Editable Area -->
              <div id="wysiwygContent"
                   contenteditable="true"
                   style="background: #0F172A; border: 1px solid #1E293B; border-radius: 0.375rem; min-height: 300px; overflow-y: auto; padding: 1rem; color: #FFFFFF; font-size: 0.875rem; line-height: 1.6;"
                   placeholder="Write your email content here..."><%= raw email_template.body %></div>
            </div>

            <!-- HTML Editor -->
            <div id="htmlEditor" style="display: none;">
              <textarea id="templateBody"
                        rows="15"
                        placeholder="Write your email HTML here..."
                        style="width: 100%; background: #0F172A; border: 1px solid #1E293B; border-radius: 0.375rem; padding: 1rem; color: #FFFFFF; font-family: 'Courier New', monospace; font-size: 0.875rem;"><%= email_template.body %></textarea>
            </div>
          </div>

          <!-- Variable Helper -->
          <div style="background: rgba(124, 58, 237, 0.1); border: 1px solid #1E293B; border-radius: 0.375rem; padding: 1rem;">
            <h6 style="color: #FFFFFF; font-weight: 600; margin-bottom: 0.75rem; display: flex; align-items: center; gap: 0.5rem;">
              <i data-lucide="variable" style="width: 1rem; height: 1rem; color: #A78BFA;"></i>
              Quick Insert Variables
            </h6>
            <div style="display: flex; gap: 0.5rem; flex-wrap: wrap;">
              <button type="button" onclick="insertVariable('{{first_name}}')" style="background: transparent; color: #A78BFA; border: 1px solid #A78BFA; padding: 0.375rem 0.75rem; border-radius: 0.375rem; cursor: pointer; font-size: 0.875rem; font-weight: 500;">
                {{first_name}}
              </button>
              <button type="button" onclick="insertVariable('{{last_name}}')" style="background: transparent; color: #A78BFA; border: 1px solid #A78BFA; padding: 0.375rem 0.75rem; border-radius: 0.375rem; cursor: pointer; font-size: 0.875rem; font-weight: 500;">
                {{last_name}}
              </button>
              <button type="button" onclick="insertVariable('{{full_name}}')" style="background: transparent; color: #A78BFA; border: 1px solid #A78BFA; padding: 0.375rem 0.75rem; border-radius: 0.375rem; cursor: pointer; font-size: 0.875rem; font-weight: 500;">
                {{full_name}}
              </button>
              <button type="button" onclick="insertVariable('{{email}}')" style="background: transparent; color: #A78BFA; border: 1px solid #A78BFA; padding: 0.375rem 0.75rem; border-radius: 0.375rem; cursor: pointer; font-size: 0.875rem; font-weight: 500;">
                {{email}}
              </button>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Right Column: Preview -->
    <div>
      <!-- Live Preview -->
      <div style="background: #1A1F35; border: 1px solid #1E293B; border-radius: 0.75rem; overflow: hidden; margin-bottom: 1.5rem;">
        <div style="background: #0F172A; border-bottom: 1px solid #1E293B; padding: 1rem; display: flex; align-items: center; gap: 0.5rem;">
          <i data-lucide="eye" style="width: 1rem; height: 1rem; color: #22D3EE;"></i>
          <h6 style="color: #FFFFFF; font-weight: 600; margin: 0; font-size: 0.875rem;">Live Preview</h6>
        </div>
        <div style="padding: 1.5rem;">
          <!-- Email Preview -->
          <div style="background: #0F172A; border: 1px solid #1E293B; border-radius: 0.375rem; padding: 1.5rem; min-height: 400px;">
            <div style="margin-bottom: 1.5rem;">
              <p style="color: #64748B; margin: 0; font-size: 0.75rem;">Subject:</p>
              <div id="previewSubject" style="color: #FFFFFF; font-weight: 600;"></div>
            </div>
            <hr style="border: none; border-top: 1px solid #1E293B; margin: 1rem 0;">
            <div id="previewBody" style="color: #FFFFFF; line-height: 1.6; font-size: 0.875rem;"></div>
          </div>
        </div>
      </div>

      <!-- Template Info -->
      <div style="background: #1A1F35; border: 1px solid #1E293B; border-radius: 0.75rem; overflow: hidden;">
        <div style="background: #0F172A; border-bottom: 1px solid #1E293B; padding: 1rem; display: flex; align-items: center; gap: 0.5rem;">
          <i data-lucide="info" style="width: 1rem; height: 1rem; color: #22D3EE;"></i>
          <h6 style="color: #FFFFFF; font-weight: 600; margin: 0; font-size: 0.875rem;">Template Information</h6>
        </div>
        <div style="padding: 1.5rem;">
          <div style="margin-bottom: 1rem;">
            <p style="color: #64748B; margin: 0; font-size: 0.75rem;">Template ID</p>
            <p style="color: #FFFFFF; margin: 0; font-weight: 500;">#<%= email_template.id %></p>
          </div>

          <div style="margin-bottom: 1rem;">
            <p style="color: #64748B; margin: 0; font-size: 0.75rem;">Created</p>
            <p style="color: #FFFFFF; margin: 0; font-weight: 500;"><%= email_template.created_at.strftime('%B %d, %Y') %></p>
          </div>

          <div>
            <p style="color: #64748B; margin: 0; font-size: 0.75rem;">Used in Campaigns</p>
            <% campaign_count = email_template.campaigns.count %>
            <% if campaign_count > 0 %>
              <span style="background: rgba(34, 197, 94, 0.2); color: rgb(74, 222, 128); padding: 0.25rem 0.75rem; border-radius: 0.375rem; font-size: 0.75rem; font-weight: 600; display: inline-block;">
                <%= pluralize(campaign_count, 'campaign') %>
              </span>
            <% else %>
              <span style="background: rgba(108, 117, 125, 0.2); color: #94A3B8; padding: 0.25rem 0.75rem; border-radius: 0.375rem; font-size: 0.75rem; font-weight: 600; display: inline-block;">
                Not used yet
              </span>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  #wysiwygContent {
    font-size: 14px;
  }

  #wysiwygContent:focus {
    outline: none;
    border-color: #A78BFA !important;
    box-shadow: 0 0 0 0.25rem rgba(167, 139, 250, 0.25);
  }

  #wysiwygContent[contenteditable]:empty::before {
    content: attr(placeholder);
    color: #64748B;
  }

  #templateBody:focus {
    outline: none;
    border-color: #A78BFA !important;
    box-shadow: 0 0 0 0.25rem rgba(167, 139, 250, 0.25);
  }

  #templateSubject:focus {
    outline: none;
    border-color: #A78BFA !important;
    box-shadow: 0 0 0 0.25rem rgba(167, 139, 250, 0.25);
  }
</style>

<script>
  // Track current editor mode
  let currentMode = 'wysiwyg';

  // Switch between editors
  function switchToWysiwyg() {
    // Get current HTML content
    const htmlContent = document.getElementById('templateBody').value;
    document.getElementById('wysiwygContent').innerHTML = htmlContent;

    // Show/hide editors
    document.getElementById('wysiwygEditor').style.display = 'block';
    document.getElementById('htmlEditor').style.display = 'none';

    // Update button states
    document.getElementById('wysiwygMode').style.background = 'linear-gradient(135deg, #7C3AED 0%, #A78BFA 100%)';
    document.getElementById('wysiwygMode').style.color = 'white';
    document.getElementById('wysiwygMode').style.border = 'none';

    document.getElementById('htmlMode').style.background = 'transparent';
    document.getElementById('htmlMode').style.color = '#22D3EE';
    document.getElementById('htmlMode').style.border = '1px solid #22D3EE';

    currentMode = 'wysiwyg';
    updatePreview();
  }

  function switchToHtml() {
    // Get current WYSIWYG content
    const wysiwygContent = document.getElementById('wysiwygContent').innerHTML;
    document.getElementById('templateBody').value = wysiwygContent;

    // Show/hide editors
    document.getElementById('wysiwygEditor').style.display = 'none';
    document.getElementById('htmlEditor').style.display = 'block';

    // Update button states
    document.getElementById('wysiwygMode').style.background = 'transparent';
    document.getElementById('wysiwygMode').style.color = '#22D3EE';
    document.getElementById('wysiwygMode').style.border = '1px solid #22D3EE';

    document.getElementById('htmlMode').style.background = 'linear-gradient(135deg, #7C3AED 0%, #A78BFA 100%)';
    document.getElementById('htmlMode').style.color = 'white';
    document.getElementById('htmlMode').style.border = 'none';

    currentMode = 'html';
    updatePreview();
  }

  // Format text in WYSIWYG editor
  function formatText(command, value = null) {
    document.execCommand(command, false, value);
    document.getElementById('wysiwygContent').focus();
    updatePreview();
  }

  // Insert link
  function insertLink() {
    const url = prompt('Enter URL:', 'https://');
    if (url) {
      formatText('createLink', url);
    }
  }

  // Initialize preview
  function updatePreview() {
    const subject = document.getElementById('templateSubject').value;
    const body = currentMode === 'wysiwyg'
      ? document.getElementById('wysiwygContent').innerHTML
      : document.getElementById('templateBody').value;

    // Update preview with sample data
    const previewData = {
      '{{first_name}}': 'John',
      '{{last_name}}': 'Doe',
      '{{full_name}}': 'John Doe',
      '{{email}}': 'john.doe@example.com'
    };

    let previewSubject = subject;
    let previewBody = body;

    // Replace variables with sample data
    Object.keys(previewData).forEach(key => {
      const regex = new RegExp(key.replace(/[.*+?^${}()|[\]\\]/g, '\\$&'), 'g');
      previewSubject = previewSubject.replace(regex, previewData[key]);
      previewBody = previewBody.replace(regex, previewData[key]);
    });

    // Simple HTML sanitization - allow only basic formatting tags
    const allowedTags = ['p', 'br', 'strong', 'b', 'em', 'i', 'u', 'a', 'ul', 'ol', 'li', 'h1', 'h2', 'h3', 'h4', 'h5', 'h6', 'div', 'span'];
    const tempDiv = document.createElement('div');
    tempDiv.innerHTML = previewBody;

    // Remove any script tags and dangerous attributes
    const scripts = tempDiv.querySelectorAll('script, style');
    scripts.forEach(el => el.remove());

    // Remove dangerous attributes
    const allElements = tempDiv.querySelectorAll('*');
    allElements.forEach(el => {
      // Remove all attributes except href for links
      Array.from(el.attributes).forEach(attr => {
        if (attr.name !== 'href' || el.tagName !== 'A') {
          el.removeAttribute(attr.name);
        }
      });
    });

    document.getElementById('previewSubject').textContent = previewSubject || '(No subject)';
    document.getElementById('previewBody').innerHTML = tempDiv.innerHTML || '(No content)';
  }

  // Insert variable at cursor position
  function insertVariable(variable) {
    if (currentMode === 'wysiwyg') {
      // Insert into contenteditable
      document.getElementById('wysiwygContent').focus();
      document.execCommand('insertText', false, variable);
    } else {
      // Insert into textarea
      const textarea = document.getElementById('templateBody');
      const start = textarea.selectionStart;
      const end = textarea.selectionEnd;
      const text = textarea.value;

      textarea.value = text.substring(0, start) + variable + text.substring(end);
      textarea.selectionStart = textarea.selectionEnd = start + variable.length;
      textarea.focus();
    }

    updatePreview();
  }

  // Save template changes
  function saveTemplateChanges() {
    const templateId = <%= email_template.id %>;
    const subject = document.getElementById('templateSubject').value;
    const body = currentMode === 'wysiwyg'
      ? document.getElementById('wysiwygContent').innerHTML
      : document.getElementById('templateBody').value;

    if (!subject.trim()) {
      alert('Please enter a subject line');
      return;
    }

    if (!body.trim()) {
      alert('Please enter email content');
      return;
    }

    // Show loading state
    const saveBtn = document.getElementById('saveTemplateChanges');
    const originalText = saveBtn.innerHTML;
    saveBtn.innerHTML = '<i data-lucide="loader-circle" class="icon-spin me-1" aria-hidden="true"></i> Saving...';
    saveBtn.disabled = true;

    // Re-initialize Lucide for the new icon
    if (typeof lucide !== 'undefined') {
      lucide.createIcons();
    }

    // Make API call to save
    fetch(`/email_templates/${templateId}`, {
      method: 'PATCH',
      headers: {
        'Content-Type': 'application/json',
        'Accept': 'application/json',
        'X-CSRF-Token': document.querySelector('meta[name="csrf-token"]')?.content ||
                        parent.document.querySelector('meta[name="csrf-token"]')?.content ||
                        '<%= form_authenticity_token %>'
      },
      body: JSON.stringify({
        email_template: {
          subject: subject,
          body: body
        }
      })
    })
    .then(response => {
      if (!response.ok && response.status !== 302) {
        throw new Error(`HTTP error! status: ${response.status}`);
      }
      // If we get a redirect (302), consider it a success since the template was saved
      if (response.status === 302 || response.redirected) {
        return { success: true };
      }
      return response.json();
    })
    .then(data => {
      // Show success message
      window.scoutSendMessage?.('✅ Email template saved successfully!');

      // If in HTML mode, sync the content back to WYSIWYG
      if (currentMode === 'html') {
        document.getElementById('wysiwygContent').innerHTML = body;
      }
    })
    .catch(error => {
      alert('Error saving template: ' + error.message);
    })
    .finally(() => {
      // Restore button state
      saveBtn.innerHTML = originalText;
      saveBtn.disabled = false;
    });
  }

  // Send test email
  function sendTestEmail() {
    const templateId = <%= email_template.id %>;
    const email = prompt('Enter email address to send test to:', '<%= current_user.email %>');

    if (!email) return;

    window.scoutSendMessage?.(`Send a test email for template ${templateId} to ${email}`);
  }

  // Add event listeners
  document.getElementById('templateSubject').addEventListener('input', updatePreview);
  document.getElementById('templateBody').addEventListener('input', updatePreview);
  document.getElementById('wysiwygContent').addEventListener('input', updatePreview);

  // Sync content when switching tabs (in case user made changes)
  document.getElementById('wysiwygContent').addEventListener('blur', function() {
    if (currentMode === 'wysiwyg') {
      document.getElementById('templateBody').value = this.innerHTML;
    }
  });

  // Initial preview
  updatePreview();

  // Lucide icon initialization
  document.addEventListener('turbo:load', () => {
    if (typeof lucide !== 'undefined') {
      lucide.createIcons();
    }
  });

  document.addEventListener('DOMContentLoaded', () => {
    if (typeof lucide !== 'undefined') {
      lucide.createIcons();
    }
  });
</script>
