<%# Scout/Amos Chat Mode Sidebar - Space Aware %>
<nav class="side-nav collapsed" data-scout-target="sideNav">
  <div class="side-nav-header">
    <div class="brand" style="background: linear-gradient(135deg, #7C3AED, #A78BFA); border-radius: 8px; padding: 6px; display: flex; align-items: center; justify-content: center;">
      <%= image_tag "Logo_mark.png", style: "height: 2rem; width: auto;", alt: "AMOS" %>
    </div>
    <button class="nav-toggle" data-action="click->scout#toggleSideNav" data-scout-target="navToggle" aria-label="Toggle navigation menu">
      <i data-lucide="menu" aria-hidden="true"></i>
    </button>
  </div>

  <div class="nav-items">
    <div class="nav-item" data-action="click->scout#loadOperationsDashboardCanvas" title="Command Center — system health, notifications, and monitoring">
      <i data-lucide="radar"></i>
      <span class="nav-text">Command Center</span>
      <span id="system-notifications-badge" class="nav-badge notification-badge" style="display: none;">0</span>
    </div>
    <div class="nav-item" data-action="click->scout#loadMyCreationsCanvas" title="Your apps, pages, and automations">
      <i data-lucide="sparkles"></i>
      <span class="nav-text">Apps & Automations</span>
    </div>
    <div class="nav-item" data-action="click->scout#loadWorkInboxCanvas" title="Agent work inbox and file downloads">
      <i data-lucide="inbox"></i>
      <span class="nav-text">Work Inbox</span>
      <span id="work-inbox-badge" class="nav-badge" style="display: none;">0</span>
    </div>
    <div class="nav-item" data-action="click->scout#loadScheduledTasksCanvas" title="Scheduled and running tasks">
      <i data-lucide="list-checks"></i>
      <span class="nav-text">Tasks</span>
      <span id="active-task-count" class="task-badge" style="display: none;">0</span>
    </div>
    <div class="nav-item" data-action="click->scout#loadContactsCanvas" title="View and manage contacts">
      <i data-lucide="users"></i>
      <span class="nav-text">Contacts</span>
    </div>
    <div class="nav-item" data-action="click->scout#loadPipelineCanvas" title="Sales pipeline">
      <i data-lucide="git-branch"></i>
      <span class="nav-text">Pipeline</span>
    </div>
    <div class="nav-item" data-action="click->scout#loadDocumentViewerCanvas" title="View documents">
      <i data-lucide="file-text"></i>
      <span class="nav-text">Documents</span>
    </div>
    <div class="nav-item" data-action="click->scout#loadIntegrationsCanvas" title="Connected services">
      <i data-lucide="plug"></i>
      <span class="nav-text">Integrations</span>
    </div>

    <%# Custom module canvases %>
    <% if current_user&.entity %>
      <% module_canvases = current_user.entity.app_modules.active.includes(:module_canvases).flat_map(&:module_canvases).select { |c| sidebar_item_visible?("module_#{c.app_module.slug}_#{c.slug}") } %>
      <% module_canvases.each do |canvas| %>
        <div class="nav-item" 
             onclick="window.scoutController && window.scoutController.loadScoutCanvas('module_<%= canvas.slug %>', {}); window.scoutController && window.scoutController.collapseSideNav()"
             title="<%= canvas.name %>">
          <i data-lucide="<%= canvas.app_module.metadata&.dig('icon') || 'database' %>"></i>
          <span class="nav-text"><%= canvas.name.truncate(20) %></span>
        </div>
      <% end %>
    <% end %>
  </div>

  <div class="nav-footer">
    <div class="nav-item" data-action="click->scout#openSettings">
      <i data-lucide="settings"></i>
      <span class="nav-text">Settings</span>
    </div>
    <div class="nav-item" data-action="click->scout#openProfile">
      <i data-lucide="user"></i>
      <span class="nav-text">My Profile</span>
    </div>
    <div class="nav-item" data-theme-toggle title="Toggle light/dark mode">
      <i data-lucide="moon" data-theme-icon></i>
      <span class="nav-text">Theme</span>
    </div>
    <div class="nav-item" data-action="click->scout#logout">
      <i data-lucide="log-out"></i>
      <span class="nav-text">Logout</span>
    </div>
  </div>
</nav>
